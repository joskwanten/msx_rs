/* Minimal stand-in for Genesis Plus GX's shared.h — just the bits
   ym2413.c needs when compiled standalone for the golden-master test. */
#include <stdint.h>
#include <string.h>
#include <math.h>
typedef uint8_t  UINT8;
typedef int8_t   INT8;
typedef uint16_t UINT16;
typedef int16_t  INT16;
typedef uint32_t UINT32;
typedef int32_t  INT32;
#define INLINE static inline
